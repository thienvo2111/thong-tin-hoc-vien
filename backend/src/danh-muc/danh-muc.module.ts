import { Module } from '@nestjs/common';

// Dịch vụ Danh mục dùng chung — xem docs/api-contract.md mục 4.
// Chưa có controller/service — sẽ thêm ở lượt triển khai kế tiếp.
@Module({})
export class DanhMucModule {}
