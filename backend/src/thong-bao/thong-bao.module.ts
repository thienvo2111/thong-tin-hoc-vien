import { Module } from '@nestjs/common';

// Dịch vụ Thông báo — xem docs/api-contract.md mục 8.
// Chưa có controller/service — sẽ thêm ở lượt triển khai kế tiếp.
@Module({})
export class ThongBaoModule {}
