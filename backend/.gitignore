node_modules
dist

# Keep environment variables out of version control
.env
.env.local

/generated/prisma

# Lưu trữ cục bộ file gốc/kết quả import (xem src/import/util/import-storage.util.ts) —
# thay thế tạm cho object storage thật, không commit dữ liệu người dùng tải lên
/storage
